@model IEnumerable<myschooldbapplication.Models.Std>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Stname)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Stname)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StdId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StdId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StdId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
